language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t jopealgorta/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true jopealgorta/docker-react npm run test
